import { expect } from "chai";
import { BigNumber } from "ethers";
import { ethers } from "hardhat";

describe("Sudoku", function () {
  it("Should deploy", async function () {
    // const SudokuExchange = await ethers.getContractFactory("SudokuExchange");
    // const sudokuExchange = await SudokuExchange.deploy();
    // await sudokuExchange.deployed();

    // const challenge = [
    //    3, 0, 6, 5, 0, 8, 4, 0, 0,
    //    5, 2, 0, 0, 0, 0, 0, 0, 0,
    //    0, 8, 7, 0, 0, 0, 0, 3, 1,
    //    0, 0, 3, 0, 1, 0, 0, 8, 0,
    //    9, 0, 0, 8, 6, 3, 0, 0, 5,
    //    0, 5, 0, 0, 9, 0, 6, 0, 0,
    //    1, 3, 0, 0, 0, 0, 2, 5, 0,
    //    0, 0, 0, 0, 0, 0, 0, 7, 4,
    //    0, 0, 5, 2, 0, 6, 3, 0, 0];

    // const SudokuChallenge = await ethers.getContractFactory("SudokuChallenge");
    // const sudokuChallenge = await SudokuChallenge.deploy(challenge);
    // await sudokuChallenge.deployed();
  });
  describe("Sudoku Exchange", async () => {});
  describe("Sudoku Challenge", async () => {});
});
